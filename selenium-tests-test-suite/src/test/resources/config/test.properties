environment=test

# Application under test (automation / isolated test deployment)
base.url=http://localhost:8081/foot
browser=chrome
headless=false
timeout.seconds=15

# Test database — separate from production to avoid data pollution
db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/bookingfootball_test?useSSL=false&useUnicode=true&characterEncoding=UTF-8
db.username=root
db.password=12345

# Seeded accounts in the test database
admin.username=admin
admin.password=admin123
user.username=User
user.password=User123

# Existing duplicated data for register negative tests
existing.username=User
existing.email=nhung@gmail.com
