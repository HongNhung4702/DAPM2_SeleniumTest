# Deprecated: environment-specific settings moved to config/test.properties and config/prod.properties.
# Active config is selected via -Denv=test (default) or -Denv=prod.
# This file is kept for reference only and is no longer loaded at runtime.
